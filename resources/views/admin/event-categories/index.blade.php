@extends('layouts.admin')
@section('title', 'Categorías de actividades')
@section('content')
<div class="page-heading"><div><h1><i class="fa-solid fa-tags"></i> Categorías</h1><p class="muted">Clasificación y color de las actividades.</p></div><a class="button ghost" href="{{ route('admin.events.index') }}"><i class="fa-solid fa-arrow-left"></i> Actividades</a></div>
<section class="stack">@foreach($categories as $category)<form class="card category-row" method="POST" action="{{ route('admin.event-categories.update', $category) }}">@csrf @method('PUT')<div class="field"><label>Nombre</label><input name="name" value="{{ $category->name }}" required></div><div class="field color-field"><label>Color</label><input name="color" type="color" value="{{ $category->color }}" required></div><span class="badge neutral">{{ $category->events_count }} actividades</span><div class="actions"><button class="button secondary" type="submit"><i class="fa-solid fa-floppy-disk"></i> Guardar</button></form><form method="POST" action="{{ route('admin.event-categories.destroy', $category) }}" onsubmit="return confirm('¿Eliminar esta categoría?')">@csrf @method('DELETE')<button class="button danger" type="submit" @disabled($category->events_count > 0)><i class="fa-regular fa-trash-can"></i></button></form></div>@endforeach</section>
<section class="card" style="margin-top:1rem"><h2><i class="fa-solid fa-plus"></i> Nueva categoría</h2><form class="category-row" method="POST" action="{{ route('admin.event-categories.store') }}">@csrf<div class="field"><label for="new_name">Nombre</label><input id="new_name" name="name" required></div><div class="field color-field"><label for="new_color">Color</label><input id="new_color" name="color" type="color" value="#002f5d" required></div><button class="button" type="submit"><i class="fa-solid fa-plus"></i> Crear categoría</button></form></section>
@endsection
